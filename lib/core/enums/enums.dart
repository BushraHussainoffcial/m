enum AdminUsersAndConsultantsStatus{
  Active,
  Blocked,
}
enum AccountRequestStatus{
  Accepted,
  Rejected,
}

enum AccountType{
  User,
  Admin,
  ServiceProvider,
  ConsultantProvider
}
///
enum AdminAppointmentsStatus{
  Confirmed,
  Concluded,
  Pending,
  Canceled,
}



enum TypeFile{
  text,
  image,
  file,
  audio,
}

enum TypeMessage{
  text,
  image,
  file, audio,
}

enum ProjectStatus{
  inProgress,
  canceled,
  completed
}